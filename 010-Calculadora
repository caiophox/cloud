def calcular(num1, num2, operador):
  resultado = 0
  if operador == 1:
    resultado = num1 + num2
  elif operador == 2:
    resultado = num1 - num2
  elif operador == 3:
    resultado = num1 * num2
  elif operador == 4:
    resultado = num1 / num2
  else:
    resultado = 0
  print(resultado)
